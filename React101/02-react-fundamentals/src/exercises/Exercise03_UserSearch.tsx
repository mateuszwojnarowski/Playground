/**
 * Exercise 03 – User Search
 *
 * TODO: Create a UserSearch component that:
 *  - Fetches users from https://jsonplaceholder.typicode.com/users on mount
 *  - Shows a loading spinner/message while fetching
 *  - Shows an error message if fetch fails
 *  - Has a search input to filter users by name
 *  - Displays user cards showing name, email, and company name
 *  - Shows "No users found" when search has no results
 */

export function UserSearch() {
  return (
    <div className="exercise-placeholder">
      <h2>Exercise 03: User Search</h2>
      <p>TODO: Implement UserSearch</p>
    </div>
  )
}
